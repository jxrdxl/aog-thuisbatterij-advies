/**
 * ExitIntentPopup - DISABLED FOR FRICTION-FREE EXPERIENCE
 * 
 * This component has been disabled to provide a friction-free experience for Facebook Ads visitors.
 * Exit-intent popups can interrupt the conversion flow and increase bounce rates.
 * 
 * Instead, we rely on:
 * - Sticky CTA bar (mobile)
 * - Footer CTAs
 * - Clear value proposition in the hero section
 * - Retargeting campaigns for users who don't convert
 */

export default function ExitIntentPopup() {
  // Disabled for friction-free experience
  return null;
}
